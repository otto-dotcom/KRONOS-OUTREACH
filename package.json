{
  "name": "kronos-outreach-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd apps/dashboard && npm install --include=dev && npm run build",
    "build": "cd apps/dashboard && npm install --include=dev && npm run build"
  },
  "devDependencies": {
    "next": "16.1.6"
  }
}
